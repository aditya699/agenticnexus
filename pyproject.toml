[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agenticnexus"
version = "0.1.0"
description = "Production-grade MCP server"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/agenticnexus"]
